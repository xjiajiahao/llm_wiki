# Research Wiki

一种用 LLM 构建个人知识库的模式。

这是一个“理念文件”，适合复制给你自己的 LLM agent 使用。它的作用是传达高层模式，具体实现则由你和 agent 在实际协作中共同补全。

## 核心想法

很多人使用 LLM 处理文档时，走的是 `RAG` 路线：上传一批文件，查询时检索相关片段，再生成答案。这种方式能工作，但有一个明显问题：LLM 每次都在重新发现知识，没有积累。

如果一个问题需要综合五篇文档里的细节，LLM 每次都要重新检索、重新拼接、重新组织。知识不会沉淀，只会在会话里短暂出现。`NotebookLM`、`ChatGPT` 文件上传，以及很多常见 `RAG` 系统，本质上都接近这个工作流。

这里的想法不一样。不是在查询时直接从原始文档检索，而是让 LLM **持续构建并维护一个持久化 wiki**。这个 wiki 由结构化、互相链接的 Markdown 页面组成，位于“你”和“原始资料”之间。

当你加入一个新来源时，LLM 不只是把它索引起来等待未来查询，而是会真正阅读它、抽取关键信息、并把这些信息整合进已有 wiki：

- 更新实体页；
- 修改主题摘要；
- 标记新旧说法的冲突；
- 强化或修正已有综合结论。

这样一来，知识是“编译一次、持续维护”，而不是“每次查询都临时重建”。

关键差异就在这里：**wiki 是一个持续累积的产物。** 交叉引用已经存在，冲突已经被标记，综合结论已经反映了你读过的内容。每增加一个来源、每提出一个问题，wiki 都会变得更丰富。

在这种模式下，你通常不亲手写 wiki，或者很少亲手写。你负责找资料、探索方向、提出问题；LLM 负责繁琐但重要的维护工作：摘要、交叉引用、归档、记账、更新一致性。实践上，可以把 Obsidian 当作 IDE，把 LLM 当作程序员，把 wiki 当作代码库。

这个模式可以用于很多场景，例如：

- **个人知识管理**：目标、健康、心理、成长记录。
- **研究主题追踪**：论文、文章、报告持续摄取，逐渐形成自己的论点结构。
- **读书伴随 wiki**：边读边沉淀人物、主题、情节和关联。
- **团队内部知识库**：会议纪要、项目文档、客户反馈、Slack 讨论持续进入同一个知识层。
- **竞品分析、尽调、旅行规划、课程笔记、兴趣专题**：凡是需要长期积累知识的场景都适用。

## 架构

可以把它看成三层：

**原始资料层**：你整理好的原始文档集合，例如文章、论文、图片、数据文件。它们是不可修改的，LLM 只读取，不改写。这是事实基底。

**wiki 层**：由 LLM 维护的一组 Markdown 页面，包括摘要、实体页、概念页、比较页、总览页和综合页。LLM 负责创建、更新、加链接、保持一致；你负责阅读和判断。

**schema 层**：例如 `AGENTS.md` 之类的规范文件，用来告诉 LLM：

- wiki 如何组织；
- 页面约定是什么；
- 如何摄取来源；
- 如何回答查询；
- 如何做维护和 lint。

这是把 LLM 从“通用聊天机器人”变成“有纪律的 wiki 维护者”的关键配置。

## 操作流程

**摄取（Ingest）**：你把一个新来源放进原始资料集合，然后让 LLM 处理。一个典型流程可能是：

1. LLM 读取来源；
2. 和你确认重点；
3. 在 `sources/` 写摘要页；
4. 更新索引；
5. 更新相关实体页和概念页；
6. 在 `log.md` 追加一条记录。

一份来源可能会改动 10 到 15 个页面。实际流程可以是一篇一篇精细摄取，也可以批量处理，取决于你的使用习惯。

**查询（Query）**：你基于 wiki 提问，LLM 先查 `index.md` 找相关页面，再读取最少必要页面，最后给出带引用的回答。查询产物本身也可以回写到 wiki，例如：

- 比较页；
- 分析笔记；
- 时间线；
- 表格；
- 幻灯片。

这样你的探索不会消失在聊天记录里，而会继续沉淀成知识资产。

**巡检（Lint）**：定期让 LLM 做健康检查，寻找：

- 页面之间的冲突；
- 已被新来源淘汰的旧结论；
- 没有入链的孤儿页；
- 被提到但没有独立页面的重要概念；
- 缺失的交叉引用；
- 需要额外搜索才能补齐的数据空洞。

随着 wiki 增长，这一步会越来越重要。

## 索引与日志

有两个特殊文件对维护特别关键：

**index.md** 面向内容组织。它是 wiki 的目录：列出所有页面、链接、摘要，并按类别组织。回答问题时，LLM 先读索引，再下钻到具体页面。在中等规模下，这种方法通常已经足够好，不一定需要额外的 embedding 检索系统。

**log.md** 面向时间顺序。它按时间追加记录发生过的事，例如：

- 摄取了什么；
- 做了什么查询；
- 跑了什么 lint；
- 做了什么大规模重构。

如果日志前缀统一，例如 `## [2026-04-02] ingest | Article Title`，就很容易用简单工具按时间回顾。

## 可选工具

随着 wiki 变大，你可能会想做一些辅助工具，帮助 LLM 更高效操作知识库。

最明显的例子是搜索。小规模时 `index.md` 就够用；规模上来以后，最好有真正的搜索引擎。比如 [qmd](https://github.com/tobi/qmd) 就是一个面向 Markdown 的本地搜索工具，支持混合检索和 LLM 重排序。你也可以自己做更轻量的脚本。

## 实用建议

- **Obsidian Web Clipper** 很适合把网页快速转成 Markdown 并放进来源层。
- **图片尽量本地化**。在 Obsidian 里把附件目录固定到例如 `raw/assets/`，这样 LLM 可以直接查看图片，不依赖可能失效的外链。
- **图谱视图很有价值**。Obsidian 的 graph view 能直观看到哪些页面是中心节点，哪些是孤儿页。
- **Markdown 幻灯片很适合作为查询产物**。例如 `Marp`。
- **如果页面前言有结构化元数据**，`Dataview` 之类的插件可以进一步生成动态目录和表格。
- wiki 本质上就是一个 Markdown git 仓库，因此天然拥有版本历史、分支和协作能力。

## 为什么这个模式有效

维护知识库最烦的部分往往不是阅读和思考，而是琐碎维护：

- 更新交叉引用；
- 同步多个页面的摘要；
- 标记新旧来源之间的冲突；
- 保持几十个页面的一致性。

人类往往不是不想用 wiki，而是维护成本增长得太快。LLM 的优势恰好就在这里：它不会嫌烦，不会忘记补链接，也可以一次性稳定修改很多文件。于是 wiki 的维护成本显著下降，知识积累就更容易持续。

在这个分工里：

- 人类负责选择来源、设定方向、提出问题、做判断；
- LLM 负责其余大部分维护性工作。

## 说明

这个文档故意保持抽象。它描述的是一种模式，而不是某个固定实现。目录结构、页面格式、schema 规则、工具链，都应该根据你的研究领域和偏好来定。

其中提到的很多东西都是可选的：

- 你的来源也许只有文本，不需要图片处理；
- 你的 wiki 也许规模很小，不需要专门搜索；
- 你也许不关心幻灯片，只想要 Markdown 页面；
- 你也许希望输出成完全不同的格式。

正确的用法不是“照抄一个模板”，而是把这份理念文件交给 LLM agent，然后一起把它实例化成适合你自己的版本。
